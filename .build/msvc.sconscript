Import('env')

copy.Append(CPPDEFINES = ['WIN32', '_VARIADIC_MAX=8'])
env.Append(CCFLAGS = [ '/EHsc' ])

if env['debug'] == True:
    env.Append(CCFLAGS = [ '/MDd' ])
else:
    env.Append(CCFLAGS = [ '/MD' ])
